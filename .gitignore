# Windows / OneDrive clutter
Thumbs.db
desktop.ini
~$*

# macOS clutter (in case the site is ever edited on a Mac)
.DS_Store

# Editor scratch files
*.swp
*.tmp

# Original full-size photo dump — kept locally for re-swapping, not published
images/incoming/

# Agent tooling artifacts — CLAUDE.md is the tracked source of truth
AGENTS.md
